- name: Generate Xcode project with XcodeGen
  working-directory: ./iOS-Expense-Tracker
  run: |
    echo "=== Generating Xcode project from project.yml ==="
    xcodegen generate
    echo "=== Project generated successfully ==="
    ls -la ExpenseTracker.xcodeproj/